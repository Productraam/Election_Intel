# ── Election Intelligence – Production Dependencies ──
# Web framework
Flask>=3.0,<4.0
Flask-SQLAlchemy>=3.1,<4.0
Werkzeug>=3.0,<4.0
gunicorn>=21.2,<24.0

# Auth
PyJWT>=2.8,<3.0

# PDF parsing & report generation
PyMuPDF>=1.24,<2.0
reportlab>=4.0,<5.0
Pillow>=10.0,<12.0

# OCR (cross-platform fallback). Windows users may also `pip install winocr`
# for ~10x faster native OCR; the parser auto-detects it.
pytesseract>=0.3.10,<1.0

# Data
numpy>=1.26,<3.0

# HTTP (for WhatsApp providers and webhooks)
requests>=2.31,<3.0

# Optional: PostgreSQL driver (uncomment if using DATABASE_URL=postgresql://…)
# psycopg2-binary>=2.9,<3.0
